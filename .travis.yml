sudo: required
services:
    - docker
    language: generic
before_install:
    - docker build -t ashishyete/docker-react -f Dockerfile.dev .
script:
    - docker run ashishyete/docker-react npm run test -- --coverage